# ==========================================
# AudiobookBay Automated Configuration
# ==========================================

# --- Torrent Client Configuration (REQUIRED) ---
# Supported clients: qbittorrent, transmission, deluge
DL_CLIENT=qbittorrent

# Connection Details
# If running in Docker, use the container name (e.g., "qbittorrent") or host IP.
DL_HOST=192.168.1.123
DL_PORT=8080
DL_USERNAME=admin
DL_PASSWORD=adminadmin

# Protocol (http or https). Default: http
DL_SCHEME=http

# Category to apply to added torrents. Default: abb-automated
DL_CATEGORY=abb-automated

# (Optional) Deluge specific: If you need to specify the full URL manually
# DL_URL=http://deluge:8112

# --- System Configuration (REQUIRED) ---

# CRITICAL: The path where files are saved *inside the Torrent Client container*.
# This app does not write to this path; it sends this string to the client.
# Example: If your qBittorrent maps /downloads to /data/downloads, set this to /downloads.
SAVE_PATH_BASE=/downloads/audiobooks

# Security: Change this to a long random string for production.
SECRET_KEY=change_this_to_a_secure_random_key

# --- Search & Scraping (Optional) ---

# Primary AudiobookBay mirror to search (e.g. audiobookbay.lu, audiobookbay.is)
ABB_HOSTNAME=audiobookbay.lu

# Comma-separated list of additional mirrors to try if the primary fails
# ABB_MIRRORS=audiobookbay.is,audiobookbay.se,audiobookbay.nl

# Max pages to scrape per search. Higher = slower. Default: 3
PAGE_LIMIT=3

# Custom Trackers: Comma-separated list of trackers to add to every magnet link.
# If not set, a default list of public trackers is used.
# MAGNET_TRACKERS=udp://tracker.opentrackr.org:1337/announce,udp://open.stealth.si:80/announce

# --- Audiobookshelf Integration (Optional) ---
# Enables the "Reload Library" button in the UI.

# URL to your Audiobookshelf instance
# ABS_URL=http://192.168.1.123:13378

# API Token (Found in ABS Settings -> Users -> Click User -> API Token)
# ABS_KEY=your_api_token_here

# Library ID (Found in the URL address bar when viewing your library in ABS)
# ABS_LIB=your_library_id_here

# --- Application Settings (Optional) ---

# Port inside the container. Default: 5078
LISTEN_PORT=5078

# Bind Host. 0.0.0.0 (IPv4), [::] (IPv6 Auto-detect), or specific IP.
LISTEN_HOST=0.0.0.0

# Logging Level: DEBUG, INFO, WARNING, ERROR. Default: INFO
LOG_LEVEL=INFO

# Timezone for logs (e.g. America/New_York). Default: UTC
TZ=UTC

# Worker Threads. Default: 8.
# Keep WORKERS=1 (hardcoded to prevent IP bans) but increase THREADS if you have many concurrent users.
THREADS=8

# --- UI Customization (Optional) ---
# Add a custom link to the navigation bar (e.g. "Open Player")
# NAV_LINK_NAME=Open Audiobookshelf
# NAV_LINK_URL=http://192.168.1.123:13378
